public class Zero extends Card{
    public Zero(int suite){//instantiates card with inputed suite and _type 0
        _suite = suite;
        _type = 0;
    }
    
    public void action(){ //empty action because nothing happens immediately when this card is played
    }
}
